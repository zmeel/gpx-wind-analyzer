services:
  gpx-wind-analyzer:
    build: .
    container_name: gpx-wind-analyzer
    restart: unless-stopped
    ports:
      - "8085:8000"
    volumes:
      - ./app:/app
      - ./uploads:/uploads
